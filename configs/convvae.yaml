model_params:
  name: 'ConvoVAE'
  in_channels: 1
  latent_dim: 72
  h_in: &h_in 51  # input's size
  h_out: &h_out 102  # output's size = 17 * 3 * 2
  tw: &w 12  # time window

data_params:
  data_path: "/data/hinguyen/smpl_dataset/DIP_IMU_and_Others/"
  train_batch_size: 128
  val_batch_size:  128
  patch_size: 4
  num_workers: 4
  tw: *w

exp_params:
  LR: 0.0005
  weight_decay: 0.0
  scheduler_gamma: 0.95
  kld_weight: 0.00025
  manual_seed: 1265
  h_in: *h_in
  h_out: *h_out
  tw: *w

trainer_params: &trainer_params
  gpus: [0]
  max_epochs: 101

logging_params:
  save_dir: "/home/hinguyen/Data/PycharmProjects/compressive-sensing-imu/logs"
  name: "ConvoVAE"
